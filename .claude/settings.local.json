{
  "permissions": {
    "allow": [
      "Bash(docker compose:*)",
      "Bash(timeout 120 bash -c \"until docker inspect keycloak --format='{{.State.Health.Status}}' 2>/dev/null | grep -q healthy; do sleep 5; echo -n '.'; done; echo ' Keycloak is healthy!'\")",
      "Bash(timeout 150 bash -c \"until docker inspect keycloak --format='{{.State.Health.Status}}' 2>/dev/null | grep -q healthy; do sleep 5; echo -n '.'; done && echo ' OK'\")",
      "Bash(timeout 150 bash -c \"until docker inspect keycloak --format='{{.State.Health.Status}}' 2>/dev/null | grep -q healthy; do sleep 5; echo -n '.'; done && echo ' Keycloak healthy!'\")",
      "Bash(curl -s -X POST http://localhost:8001/api/auth/token/login -H \"Content-Type: application/json\" -d \"{\\\\\"username\\\\\":\\\\\"admin\\\\\",\\\\\"password\\\\\":\\\\\"admin123\\\\\"}\")",
      "Bash(docker exec *)",
      "Bash(docker restart *)",
      "Bash(curl -s http://localhost:8005/api/ai/health -H \"Authorization: Bearer test\")",
      "Bash(docker stop *)",
      "Bash(docker rm *)",
      "Bash(docker-compose up *)",
      "Bash(curl -s http://localhost:8080/realms/ent-est-sale/protocol/openid-connect/certs)"
    ]
  }
}
